# mobius-user runtime dependencies (Cloud Run / FastAPI service)

# Core auth
sqlalchemy>=2.0
psycopg[binary]>=3.0
bcrypt>=4.0
PyJWT>=2.0
python-dotenv>=1.0

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.6
pydantic>=2.5

# Google sign-in (ID-token verify-only)
google-auth>=2.28

# HTTP client for calling internal skills (welcome email via mobius-skills/email)
requests>=2.31

# Migrations (entrypoint can optionally run alembic upgrade head)
alembic>=1.10
